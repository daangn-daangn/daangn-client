import { css } from "@emotion/react";

const GlobalStyle = css`
  * {
    margin: 0;
    padding: 0;
  }

  body {
    box-sizing: border-box;
    max-width: 512px;
    margin: 0 auto;
  }
`

export default GlobalStyle;